import SwiftUI

extension Project {
    struct Tab: View {
        var body: some View {
            ZStack {
                RoundedRectangle(cornerRadius: Metrics.corners)
                    .fill(Color(.secondarySystemBackground))
                ScrollView {
                    
                }
            }
            .padding(.horizontal)
        }
    }
}
